# Database Config
db.driverClassName=org.postgresql.ds.PGSimpleDataSource
db.url=${db.url}
db.username=${db.username}
db.password=${db.password}

# Run migration
db.migration=${db.migration}

# Server
server.port=${server.port}

# host (start server)
server.host=${server.host}




#new relic key 398b21f1ac02fb7ae01a0a1dff692cf1ba78NRAL